# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.pnpm-store/

# Next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Build
dist/
build/

# Env — .env.example is tracked; runtime secrets are NOT
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local
.vercel

# Apps-in-Toss mTLS 클라이언트 인증서 (시크릿 — 절대 커밋 금지, 배포 시 env(PEM)로 주입)
mTLS_*.zip
*인증서*.zip

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
*.pem

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test
coverage/
.nyc_output/
playwright-report/
test-results/

# Verification reports (regenerate locally)
tests/fixtures/ssaju_verification_report.json
reports/

# KASI 부분 진행 (재시도용 임시 파일)
tests/fixtures/.kasi_partial.json

# TWA
android/
twa-manifest.json
*.keystore
.gstack/

# Claude Code session cache
.claude/

# Codex local workspace artifacts
.codex/

# Playwright MCP cache
.playwright-mcp/

# Manual QA smoke screenshots
smoke-*.png

# UIDesign reference prototype (local-only, read-only per §1.6)
UIDesign/

# Canvas Type D redesign reference (local-only, consumed by bdbf86c)
coupleUnse-redesign/

# Local QA flow screenshots (generated by /qa runs, not source)
twoday-*.png
